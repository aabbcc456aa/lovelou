$("#old_replies").html('<%= escape_javascript render :partial => "old_replies" ,:locals => {:old_replies => @old_replies} %>' );
$("#content").attr("value","");
$("#button").attr("disabled","disabled");
